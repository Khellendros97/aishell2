1、安装包增加自动安装git bash的过程
2、侧边栏服务器列表中，服务器卡片增加编辑按钮，可以快捷编辑服务器配置
3、在AI对话的命令建议卡片中点击‘粘贴到终端’后，需要自动聚焦到终端，方便用户直接敲回车执行命令
4、ssh如果因为密码、密钥问题连接失败，弹出对话框让用户重新设置用户名、认证方式、密码、密钥，并将报错信息输出到对话框中防止被遮挡
5、SFTP中通过迷你终端执行命令在部分用户的电脑上会因为时序问题失败，改为直接通过ssh执行命令，不走小终端。将命令执行结果输出到debug窗口
6、命令收藏新增搜索、分类功能，和服务器分类逻辑类似。
7、命令收藏设置中新增’全局可用‘复选框，勾选后命令收藏可在所有项目中使用
8、AI对话中的会话列表现在是全局的，应该改到项目级
9、文件编辑器中增加ctrl+f查找、替换功能
10、修复AI对话面板中输入ctrl+c无法复制对话内容的问题
11、AI的模式切换必须新建会话才能生效，改为实时生效
12、建议模式下，给AI提供工具，让它可以主动申请切换到Agent模式，需要用户同意（不允许申请切换到SOLO模式）
13、将AI模式、思考强度文本都改为中文
  Suggent - 仅建议
  Agent - 工作
  SOLO - 全自动
  low - 低
  high - 高
  max - 最高
14、服务器卡片以及终端的右键菜单中，增加一个添加到chat按钮（服务器卡片中使用图标按钮），点击后在对话框中增加一个引用服务器的@标签，标签格式为 @remote:服务器名称 或 @local
15、系统设置中增加‘自动切换AI工作区域’按钮，开启后，在对话框中增加一个固定的工作区域标签，默认为本地，当用户打开ssh、本地终端，或是工作区域切换标签页到一个终端时，工作区域自动切换到相应服务器或本地
