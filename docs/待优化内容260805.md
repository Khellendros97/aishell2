1. 服务器列表需要增加目录分类功能（xshell有类似功能，导入时请将目录结构也一起导入）
2. 所有服务器列表需要增加搜索功能（也包括新建/编辑项目中的服务器列表）
3. 目前一台服务器只能打开一个终端标签页，需要支持多个终端并行启动。并且右键标签页可以用‘复制ssh渠道’功能
4. 历史区块增加清空功能
5. 文件资源管理器右键菜单添加‘属性’按钮，可查看文件属性
6. SFTP右键菜单增加以下功能：
  - 属性
  - 权限设置
  - 赋予可执行权限
  如果要通过ssh执行命令，请通过迷你终端来执行